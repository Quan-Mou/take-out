<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">
    <insert id="add">
        insert into shopping_cart(name,image,user_id,dish_id,setmeal_id,
                                  dish_flavor,number,create_time,amount)
        values (#{name},#{image},#{userId},#{dishId},#{setmealId},
                #{dishId},#{number},#{createTime},#{amount})
    </insert>

    <update id="update">
        update shopping_cart
        <set>
            <if test="number != null">number = #{number}</if>
        </set>
    </update>

    <select id="queryDishOrSetmeal" resultType="com.sky.entity.ShoppingCart">
        select * from shopping_cart
        <where>
            <if test="name != null">name = #{name}</if>
            <if test="image != null">and image = #{image}</if>
            <if test="dishId != null">and dish_id = #{dishId}</if>
            <if test="setmealId != null">and setmeal_id = #{setmealId} </if>
            <if test="userId != null">and user_id = #{userId}</if>
        </where>
    </select>
</mapper>
